"use client";

export function ModeToggle({ className: _className }: { className?: string }) {
  // Single unified theme locked to light mode (no dark mode per project design system).
  return null;
}
